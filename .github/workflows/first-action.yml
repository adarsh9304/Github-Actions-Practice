name: First workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on:ubuntu-latest
    steps:
      - name: print welcome
        run: echo "Welcome to our page"
      - name: print goodbye
        run: echo "Goodbye and thanks for visit"
